<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"><title>Apache DataFusion Blog - Tim Saucer (rerun.io)</title><link href="https://datafusion.apache.org/blog/" rel="alternate"/><link href="https://datafusion.apache.org/blog/feeds/tim-saucer-rerunio.atom.xml" rel="self"/><id>https://datafusion.apache.org/blog/</id><updated>2026-05-22T00:00:00+00:00</updated><entry><title>Writing Agent Skills for an Open Source Project: Lessons from DataFusion Python</title><link href="https://datafusion.apache.org/blog/2026/05/22/writing-agent-skills" rel="alternate"/><published>2026-05-22T00:00:00+00:00</published><updated>2026-05-22T00:00:00+00:00</updated><author><name>Tim Saucer (rerun.io)</name></author><id>tag:datafusion.apache.org,2026-05-22:/blog/2026/05/22/writing-agent-skills</id><summary type="html">&lt;!--
{% comment %}
Licensed to the Apache Software Foundation (ASF) under one or more
contributor license agreements.  See the NOTICE file distributed with
this work for additional information regarding copyright ownership.
The ASF licenses this file to you under the Apache License, Version 2.0
(the "License"); you may not use this file except in compliance with
the License.  You may obtain a copy of the License at

http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
{% endcomment %}
--&gt;

&lt;p&gt;If you maintain an open source project, a growing fraction of the people
using your library are not typing code directly anymore — they are asking an
agent to write it for them. That agent leans on whatever it picked up during
training, which is rarely the idiomatic style your project …&lt;/p&gt;</summary><content type="html">&lt;!--
{% comment %}
Licensed to the Apache Software Foundation (ASF) under one or more
contributor license agreements.  See the NOTICE file distributed with
this work for additional information regarding copyright ownership.
The ASF licenses this file to you under the Apache License, Version 2.0
(the "License"); you may not use this file except in compliance with
the License.  You may obtain a copy of the License at

http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
{% endcomment %}
--&gt;

&lt;p&gt;If you maintain an open source project, a growing fraction of the people
using your library are not typing code directly anymore — they are asking an
agent to write it for them. That agent leans on whatever it picked up during
training, which is rarely the idiomatic style your project actually wants.
The result is code that runs but reads like a stranger wrote it, or code
that doesn't run at all because the agent guessed at an API that doesn't
exist. You can fix this from inside the repository, with a small number of
&lt;strong&gt;agent skills&lt;/strong&gt; checked in alongside your code.&lt;/p&gt;
&lt;p&gt;This post is about how we did exactly that in
&lt;a href="https://datafusion.apache.org/python/"&gt;&lt;code&gt;datafusion-python&lt;/code&gt;&lt;/a&gt;. The specifics — DataFrame APIs,
PyO3-wrapped Rust bindings, an analytics library written on top of Apache
Arrow — are particular to our project, but the techniques are not. The
question of &lt;em&gt;who&lt;/em&gt; a skill is for and how that shapes its contents, the
question of &lt;em&gt;where&lt;/em&gt; a skill should live in the repo so the right people
load it, the question of &lt;em&gt;how to keep it honest&lt;/em&gt; as your API evolves, and
the question of &lt;em&gt;how to evaluate it&lt;/em&gt; against a real workload all generalize
to almost any library complex enough that an agent will struggle with it.&lt;/p&gt;
&lt;p&gt;Concretely, you will get out of this post:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;A pattern for splitting skills by audience — user-facing vs.
  contributor-facing — and why the split matters more than it sounds.&lt;/li&gt;
&lt;li&gt;A workflow for keeping skills in sync with a moving API by treating the
  skill itself as the maintenance tool.&lt;/li&gt;
&lt;li&gt;A method for grounding the user-facing skill against a corpus of known
  problems with known answers, run in a way that actually tests the skill
  instead of the agent's memory.&lt;/li&gt;
&lt;li&gt;A set of habits for evaluating and iterating on skills that apply to any
  project doing this work.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="what-is-an-agent-skill"&gt;What is an Agent Skill?&lt;a class="headerlink" href="#what-is-an-agent-skill" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;A skill is a Markdown file (conventionally &lt;code&gt;SKILL.md&lt;/code&gt;) with YAML frontmatter
that tells an AI coding assistant when and how to use it. The file lives in
your repository, and any agent that supports the skill ecosystem
(&lt;a href="https://claude.com/claude-code"&gt;Claude Code&lt;/a&gt;, &lt;a href="https://cursor.com"&gt;Cursor&lt;/a&gt;, &lt;a href="https://openai.com/codex/"&gt;Codex&lt;/a&gt;, &lt;a href="https://github.com/google-gemini/gemini-cli"&gt;Gemini CLI&lt;/a&gt;, &lt;a href="https://aider.chat"&gt;Aider&lt;/a&gt;, and many more)
will pull the skill in when the user is working on a relevant task.&lt;/p&gt;
&lt;p&gt;A skill is not documentation for humans. It is a focused, dense piece of
prose written &lt;em&gt;for the model&lt;/em&gt;, optimized for the moment the model is about
to generate code. That distinction matters: a good user guide is patient and
walks the reader through concepts; a good skill is opinionated and tells the
model the exact pattern to emit.&lt;/p&gt;
&lt;h2 id="two-audiences-two-skills"&gt;Two Audiences, Two Skills&lt;a class="headerlink" href="#two-audiences-two-skills" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;The single most important decision we made was to split skills into &lt;strong&gt;two
clearly separate audiences&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;End users&lt;/strong&gt; of &lt;code&gt;datafusion-python&lt;/code&gt; are people writing application code:
loading Parquet files, building DataFrame queries, computing aggregates,
calling window functions. They want the agent to produce idiomatic
&lt;code&gt;SessionContext&lt;/code&gt; / &lt;code&gt;DataFrame&lt;/code&gt; / &lt;code&gt;Expr&lt;/code&gt; code that runs on their data.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Developers&lt;/strong&gt; of &lt;code&gt;datafusion-python&lt;/code&gt; are the maintainers of the library
itself: people adding bindings, syncing with upstream &lt;a href="https://datafusion.apache.org"&gt;Apache DataFusion&lt;/a&gt;,
auditing API coverage, and refining the Python ergonomics of the
PyO3-wrapped Rust code. They want the agent to help them find gaps in the
binding layer and apply the fixes.&lt;/p&gt;
&lt;p&gt;These two audiences need almost disjoint guidance. A user does not need to
know that &lt;code&gt;python/datafusion/functions.py&lt;/code&gt; wraps &lt;code&gt;crates/core/src/functions.rs&lt;/code&gt;,
or how to grep &lt;code&gt;~/.cargo/registry&lt;/code&gt; for the upstream &lt;code&gt;invoke_with_args()&lt;/code&gt;
implementation. A maintainer does not need a SQL-to-DataFrame migration
table. Mixing the two produces a skill that is too long for both audiences
and unfocused for either.&lt;/p&gt;
&lt;p&gt;The other reason to keep them separate is &lt;strong&gt;load semantics&lt;/strong&gt;. Skills are
loaded into the model's context window. Every kilobyte of skill consumes
tokens the user could have spent on their actual code. When you publish a
skill, you should be deliberate about the audience that pays that cost.&lt;/p&gt;
&lt;h3 id="where-each-skill-lives-in-the-repo"&gt;Where Each Skill Lives in the Repo&lt;a class="headerlink" href="#where-each-skill-lives-in-the-repo" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;We landed on the following layout in &lt;code&gt;datafusion-python&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;skills/
  datafusion_python/
    SKILL.md          # user-facing skill (722 lines)

.ai/skills/
  check-upstream/
    SKILL.md          # developer skill: API parity audit
  make-pythonic/
    SKILL.md          # developer skill: ergonomic refactors
  audit-skill-md/
    SKILL.md          # developer skill: keep the user skill in sync
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The user-facing skill lives at the top level under &lt;code&gt;skills/&lt;/code&gt;, where the
skill-ecosystem tooling looks for it. This is what an end user installs.
The developer skills live under &lt;code&gt;.ai/skills/&lt;/code&gt; — they are checked into the
repo so contributors who clone it get them automatically, but they are
&lt;strong&gt;not&lt;/strong&gt; part of the public, installable skill surface.&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;.ai/skills/&lt;/code&gt; path is not a discovery convention agents look for on
their own, so we point at it explicitly from &lt;code&gt;AGENTS.md&lt;/code&gt; at the repo
root. An agent dropped into the repository reads &lt;code&gt;AGENTS.md&lt;/code&gt; first,
finds the pointer, and can then pull in the right developer skill for
the task it has been asked to do. If you adopt this layout, updating
&lt;code&gt;AGENTS.md&lt;/code&gt; to advertise the directory is what makes the developer
skills actually reachable.&lt;/p&gt;
&lt;p&gt;Following the &lt;code&gt;skills/&amp;lt;project-name&amp;gt;/SKILL.md&lt;/code&gt; convention has one immediate
payoff: installation becomes a single command. A user can wire the skill
into their agent with:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;npx skills add apache/datafusion-python
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The tool reads the repo, finds the skill at the conventional path, and
installs only that subtree — no need to clone the whole project just to
get a Markdown file. If you publish your user-facing skill in this layout,
your users get the same one-line install for free.&lt;/p&gt;
&lt;h3 id="developers-can-and-should-use-the-user-skill-too"&gt;Developers Can (and Should) Use the User Skill Too&lt;a class="headerlink" href="#developers-can-and-should-use-the-user-skill-too" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The separation is asymmetric. Maintainers absolutely benefit from loading
the user-facing skill alongside the developer skills — it tells them what
idiomatic usage &lt;em&gt;should&lt;/em&gt; look like, which is exactly the standard they need
to hold new bindings to. But end users have no reason to load the developer
skills. Their context window is better spent on the user skill plus their
own code.&lt;/p&gt;
&lt;h2 id="building-the-user-facing-skill"&gt;Building the User-Facing Skill&lt;a class="headerlink" href="#building-the-user-facing-skill" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;The hard question, once you've decided to write a user-facing skill, is
&lt;em&gt;what goes in it&lt;/em&gt;. A naive approach is to start from your existing user
guide and condense — but a user guide is organized for a human reading
top-to-bottom, and a skill needs to be organized for a model that is
about to emit a specific kind of code.&lt;/p&gt;
&lt;p&gt;Two principles shaped how we approached the writing itself, and they
matter as much as the structure of the document:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Have an agent write the skill — but feed it expert knowledge.&lt;/strong&gt;
Agents have a strong intuition for what &lt;em&gt;another agent&lt;/em&gt; needs to see in
order to produce correct code. They know which conventions are
non-obvious, which API edges are surprising, which idioms a model would
fail to infer. Use that. The skill files in &lt;code&gt;datafusion-python&lt;/code&gt; were
drafted by an agent, not hand-written.&lt;/p&gt;
&lt;p&gt;The catch is that the agent does not know your project. It does not know
which abstractions your users actually touch, which patterns you consider
idiomatic, which historical mistakes the library has accumulated. That
knowledge lives in the maintainers' heads. The initial conversation
between the author and the drafting agent is therefore a &lt;strong&gt;knowledge
capture exercise&lt;/strong&gt;: the author supplies the priorities and constraints,
the agent turns them into structured guidance. Every iteration that
follows is the same exercise on a smaller scale — every time the skill
fails in the field, the fix is more captured expertise.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Debug the skill by replaying it.&lt;/strong&gt;
When you catch the skill producing a bad output, you do not have to
guess why. Hand the agent the version of the skill that was in use at
the time of the failure, paste in the original prompt, and ask it to
explain what guidance it was following and where the guidance was
silent. Pinning the skill to a specific commit during this analysis is
important — the skill you have today is not the skill the agent had
when it made the mistake. The agent is good at pointing at the exact
gap; once you know the gap, the fix usually writes itself.&lt;/p&gt;
&lt;p&gt;With those two principles in place, we arrived at the contents of
&lt;code&gt;skills/datafusion_python/SKILL.md&lt;/code&gt; through three passes, in this order:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Pass 1: inventory the public surface.&lt;/strong&gt;
Before writing prose, list the abstractions a user actually touches. For
us that was four: &lt;code&gt;SessionContext&lt;/code&gt; (the entry point), &lt;code&gt;DataFrame&lt;/code&gt; (the
query builder), &lt;code&gt;Expr&lt;/code&gt; (expression nodes), and &lt;code&gt;functions&lt;/code&gt; (the built-in
library). This list is exactly the kind of thing the agent cannot derive
on its own — the project's public Python API is much larger than what a
typical user reaches for, and the difference is a maintainer judgment
call. We told the drafting agent which four surfaces mattered; it
organized the skill around them. Anything outside that list is either
internal or advanced enough that a user-facing skill should not be the
place to teach it. The inventory is the skill's skeleton — every later
edit hangs off one of these surfaces.&lt;/p&gt;
&lt;p&gt;One useful input here is your existing &lt;strong&gt;online user guide&lt;/strong&gt;. A
hand-written user guide has already done a version of this filtering for
you: the maintainer who wrote it chose what to introduce, what order to
introduce it in, and where to slow down and flag a footgun. We fed our
user guide to the drafting agent as a source of signal — both for
"which APIs are important enough to teach" and for "which pitfalls have
already burned real users." Many of the warnings in the final skill
trace back to a sentence somewhere in the user guide that says "be
careful with this."&lt;/p&gt;
&lt;p&gt;Be deliberate about &lt;em&gt;which&lt;/em&gt; docs you feed in, though. &lt;strong&gt;Do not use
auto-generated API reference docs&lt;/strong&gt; for this pass. Generated docs cover
the entire public surface and therefore filter nothing — handing them to
the agent will produce a skill that tries to teach everything and
teaches nothing well. The user guide is useful precisely because a human
already pruned it.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Pass 2: write the happy path for each surface.&lt;/strong&gt;
For each abstraction on the list, write the minimum code an idiomatic
user would write: how to load data, how to project columns, how to
filter, how to aggregate, how to join, how to call a window function.
The goal is not exhaustiveness; it is to give the model a &lt;em&gt;template&lt;/em&gt; it
can pattern-match against. If your project has a strong opinion about
the right way to do something (we prefer plain column-name strings over
&lt;code&gt;col("name")&lt;/code&gt; in projections, for example), this is where the opinion
goes.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Pass 3 — the long one: encode every mistake the agent makes.&lt;/strong&gt;
This is where most of the actual value of the skill comes from, and it
is where you cannot shortcut. Use the draft from passes 1 and 2 in a
real agent session. Have the agent write code against your library.
Watch what it gets wrong. Every wrong thing is a candidate skill edit.&lt;/p&gt;
&lt;p&gt;In our case, two distinct categories of guidance fell out of this loop.&lt;/p&gt;
&lt;p&gt;The first is &lt;strong&gt;outright pitfalls&lt;/strong&gt; — places where the natural agent
guess produces code that is incorrect or silently wrong:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;&amp;amp;&lt;/code&gt; / &lt;code&gt;|&lt;/code&gt; / &lt;code&gt;~&lt;/code&gt; for boolean composition, not Python's &lt;code&gt;and&lt;/code&gt; / &lt;code&gt;or&lt;/code&gt; /
  &lt;code&gt;not&lt;/code&gt;. Using the keyword forms looks syntactically fine and even runs,
  but it does not compose &lt;code&gt;Expr&lt;/code&gt; objects the way the user intended.&lt;/li&gt;
&lt;li&gt;Case sensitivity: &lt;code&gt;select("Name")&lt;/code&gt; lowercases the identifier; embed
  inner double quotes (&lt;code&gt;select('"MyCol"')&lt;/code&gt;) for case-preserved lookup.
  Without the inner quotes, the lookup fails with &lt;code&gt;No field named mycol&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Both of these were already called out in our online user guide as
footguns. Pass 1 surfaced them from the docs, which is exactly the kind
of payoff the user-guide step is meant to produce — the maintainer who
wrote the guide had already done the work of cataloguing them.&lt;/p&gt;
&lt;p&gt;The second category is &lt;strong&gt;idiomatic vs. non-idiomatic style&lt;/strong&gt;. These are
not bugs; the agent's first guess produces code that runs and returns
the right answer. But it does not read like code a maintainer would
write, and over time it diverges from the patterns the rest of the
project uses:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;col("a") &amp;gt; 10&lt;/code&gt; rather than &lt;code&gt;col("a") &amp;gt; lit(10)&lt;/code&gt; — raw Python values
  on the right-hand side of an operator are auto-wrapped into literals.&lt;/li&gt;
&lt;li&gt;Plain column names as strings in &lt;code&gt;select()&lt;/code&gt;, &lt;code&gt;sort()&lt;/code&gt;, &lt;code&gt;aggregate()&lt;/code&gt; —
  reach for &lt;code&gt;col(...)&lt;/code&gt; only when the projection needs an expression.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;HAVING&lt;/code&gt; is the &lt;code&gt;filter=&lt;/code&gt; keyword on the aggregate function, not a
  post-aggregation &lt;code&gt;filter()&lt;/code&gt; call.&lt;/li&gt;
&lt;li&gt;Semi/anti joins instead of &lt;code&gt;EXISTS&lt;/code&gt; / &lt;code&gt;NOT EXISTS&lt;/code&gt; correlated
  subqueries.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;These idiomatic rules are not in the user guide as a flat list — they
are scattered across docstrings, examples, and the implicit knowledge of
the maintainers. They show up in the skill because we watched an agent
write the non-idiomatic version and then went and wrote the rule down.
The contents of this list are not a property of &lt;code&gt;datafusion-python&lt;/code&gt;;
they are a property of &lt;em&gt;what agents guess when they haven't seen your
library before&lt;/em&gt;, and the only way to discover it is to put the skill in
front of a fresh agent and watch.&lt;/p&gt;
&lt;p&gt;A useful trick during pass 3: when the agent does get something right
in a non-obvious way, ask it &lt;em&gt;why&lt;/em&gt;. If the answer references something
that is not in your draft skill — a docstring it found, a public docs
page, a pattern from a similar library — that is a hint that the skill
is silent on something it should cover. Codify the reasoning, don't
rely on the agent finding it again next time.&lt;/p&gt;
&lt;p&gt;The next two sections describe two different things we did after the
initial draft: a one-time grounding exercise against the TPC-H corpus
to validate the skill end-to-end, and a set of developer-side skills
that flag user-skill drift whenever the API moves.&lt;/p&gt;
&lt;h2 id="grounding-the-skill-tpc-h-as-a-one-time-validation"&gt;Grounding the Skill: TPC-H as a One-Time Validation&lt;a class="headerlink" href="#grounding-the-skill-tpc-h-as-a-one-time-validation" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;A draft skill needs to be tested against something more demanding than the
ad-hoc prompts the author used while writing it. We needed a way to confirm
that the skill, once handed to a fresh agent, actually produced code that
&lt;em&gt;ran&lt;/em&gt; and returned &lt;em&gt;correct answers&lt;/em&gt; on real workloads — not just on the
five-line examples the author already had in mind. The plan, laid out in
&lt;a href="https://github.com/apache/datafusion-python/issues/1394"&gt;issue #1394&lt;/a&gt;, was a one-time end-to-end validation pass against the
&lt;strong&gt;&lt;a href="https://www.tpc.org/tpch/"&gt;TPC-H benchmark suite&lt;/a&gt;&lt;/strong&gt;, with the discoveries folded back into
the skill itself.&lt;/p&gt;
&lt;p&gt;TPC-H is attractive for this purpose because:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The benchmark ships &lt;strong&gt;plain-English problem statements&lt;/strong&gt; for each of the
   22 queries.&lt;/li&gt;
&lt;li&gt;The benchmark ships &lt;strong&gt;reference answers&lt;/strong&gt; for scale factor 1 (the
   &lt;code&gt;answers_sf1/&lt;/code&gt; directory in &lt;code&gt;examples/tpch/&lt;/code&gt;), so any candidate
   implementation can be checked for correctness automatically.&lt;/li&gt;
&lt;li&gt;The queries cover a wide cross-section of the API: aggregates, joins,
   window functions, set operations, date arithmetic, subqueries, and so
   on.&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id="what-makes-a-good-corpus"&gt;What Makes a Good Corpus&lt;a class="headerlink" href="#what-makes-a-good-corpus" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Most projects do not have a TPC-H equivalent sitting on the shelf. The
useful thing to extract from our experience is the shape of the corpus,
not the specific benchmark. Three properties matter:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;A text description of what to build, in the language of the
   problem.&lt;/strong&gt; Not pseudocode, not an API call sketch — a natural-language
   statement of &lt;em&gt;what the user wants to compute&lt;/em&gt;, the way a real user
   would phrase it. The skill is what should bridge the gap from English
   to your library's API. If the corpus already names your APIs, you are
   no longer testing the skill.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;A check that runs automatically.&lt;/strong&gt; Without that, you cannot iterate.
   The check can be a reference answer to diff against (TPC-H's
   approach), a property test, a snapshot, or even another agent acting
   as a judge — whatever lets you say &lt;em&gt;correct&lt;/em&gt; or &lt;em&gt;not correct&lt;/em&gt; without
   a human in the loop for each pass.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Coverage of the surface the skill is supposed to teach.&lt;/strong&gt; A corpus
   that hits only one or two abstractions will only validate one or two
   sections of the skill. Spread across the public API you actually want
   users to use.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;If you do not have a benchmark like TPC-H, the easiest place to start is
&lt;strong&gt;your own repository's examples&lt;/strong&gt;. Pick the existing example files, write
a plain-English description of what each one is meant to do, and see if a
fresh agent can reproduce the example from the description alone, using
only your skill and docs. Any divergence — wrong code, non-idiomatic
code, hallucinated APIs — is a hole in the skill. The example files are
already your ground truth; you just need to rewrite their &lt;em&gt;inputs&lt;/em&gt; in a
form that does not give the answer away.&lt;/p&gt;
&lt;p&gt;It helps to frame the whole exercise as &lt;strong&gt;test-driven development for
documentation&lt;/strong&gt;. The test is: given nothing but a well-written problem
statement, can a fresh agent produce correct, idiomatic code using only
your skill? When the answer is no, the skill is the thing that has to
change. Each pass is a regression test on the prose.&lt;/p&gt;
&lt;h3 id="the-evaluation-loop"&gt;The Evaluation Loop&lt;a class="headerlink" href="#the-evaluation-loop" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The corpus is structured so the agent gets the &lt;em&gt;problem&lt;/em&gt;, not the SQL:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;examples/tpch/
  q01_pricing_summary_report.py   # docstring contains the English problem statement
  q02_minimum_cost_supplier.py
  ...
  answers_sf1/
    q1.tbl                        # reference answers (the ground truth)
    q2.tbl
    ...
  _tests.py                       # diff candidate output against q*.tbl
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;We had the agent write each query as idiomatic DataFrame code, then ran the
test harness in &lt;code&gt;_tests.py&lt;/code&gt; to diff its output against the reference
answers. When the agent's code disagreed with the ground truth, that was
either a bug in the generated code, a bug in the skill, or — occasionally
— a documented behavioral difference in DataFusion that needed a comment in
the example. The loop kept running until the agent could produce correct
output for all 22 queries.&lt;/p&gt;
&lt;h3 id="forbidding-shortcuts"&gt;Forbidding Shortcuts&lt;a class="headerlink" href="#forbidding-shortcuts" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The interesting wrinkle was making the evaluation &lt;em&gt;actually evaluate the
skill&lt;/em&gt;, not the agent's ability to find a cached answer somewhere. TPC-H
has been around since the 1990s; reference SQL implementations are all over
the public web, and there are existing Python solutions in the repository's
own git history. If the agent leaned on any of those, the test would prove
nothing.&lt;/p&gt;
&lt;p&gt;We addressed this in three ways:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Restart the session frequently.&lt;/strong&gt; Each evaluation pass was run in a
   fresh agent session, with no memory of prior solutions and no inferred
   context from earlier turns. Prior conversation is leakage — the agent
   might "remember" the right answer instead of deriving it from the skill.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Explicitly forbid the shortcuts in the prompt.&lt;/strong&gt; The agent was told:
   no looking at any existing Python solutions in the repo, no SQL-based
   solutions (whether in the repo, on the web, or in your training data),
   and no prior memories. Only the docstrings, the skill, and the published
   &lt;code&gt;datafusion-python&lt;/code&gt; user documentation are fair game.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Forbid the agent from correcting its initial guess.&lt;/strong&gt; The first
   pass — the one before the agent has run its code, seen an error, and
   debugged — is the one that actually exercises the skill. Once the
   agent gets to iterate, its general debugging ability starts to
   compensate for whatever the skill failed to teach, and the
   evaluation stops measuring the skill at all. We wanted the failures.&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The second rule is worth dwelling on. There is a real temptation, when
an agent is stuck, to let it "peek" at a known-good answer just to make
progress. Don't. The whole point of the TPC-H corpus is to surface the
places where the skill is silent or wrong, and an agent that has already
seen the answer will paper over exactly those gaps.&lt;/p&gt;
&lt;h3 id="human-review-of-the-generated-code"&gt;Human Review of the Generated Code&lt;a class="headerlink" href="#human-review-of-the-generated-code" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Once the agent could produce &lt;em&gt;correct&lt;/em&gt; output for a query, the work was
only half done. Correctness is not the same as idiomatic. We then went
through each of the 22 generated scripts by hand and worked with the agent
to refactor them into the style the skill is supposed to teach: plain
column names where possible, &lt;code&gt;filter=&lt;/code&gt; on aggregates instead of
post-aggregation filters, semi/anti joins instead of &lt;code&gt;EXISTS&lt;/code&gt;, and so on.&lt;/p&gt;
&lt;p&gt;Every time we caught the agent reaching for a non-idiomatic pattern, we
asked the same question: &lt;em&gt;did the skill teach this, or did the agent
infer it?&lt;/em&gt; When the answer was "inferred," that was a gap in the skill, and
we updated &lt;code&gt;SKILL.md&lt;/code&gt; to close it.&lt;/p&gt;
&lt;h2 id="the-developer-skills"&gt;The Developer Skills&lt;a class="headerlink" href="#the-developer-skills" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;The user skill exists to teach agents how to write good user code. The
developer skills, in &lt;code&gt;.ai/skills/&lt;/code&gt;, exist to help maintainers keep the
project itself in good shape.&lt;/p&gt;
&lt;p&gt;We ended up with three of them. The number was not planned up front;
each skill was written in response to a recurring chore that a
maintainer kept doing by hand and getting wrong in the same ways every
time. Once a task has a predictable shape and a checklist that a careful
person would follow, it is a candidate for a skill — and the act of
writing the skill forces you to make the checklist explicit.&lt;/p&gt;
&lt;p&gt;The three correspond to the three places maintenance drift shows up in a
binding project like ours:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;check-upstream&lt;/code&gt;&lt;/strong&gt; — &lt;em&gt;the public API of the wrapped library moved
  and we didn't keep up.&lt;/em&gt; Run after every upstream sync to find
  functions, methods, and types that exist in the Rust DataFusion
  library but were never exposed in Python.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;make-pythonic&lt;/code&gt;&lt;/strong&gt; — &lt;em&gt;the binding works, but it doesn't feel like
  Python.&lt;/em&gt; Audit function signatures for places where a user has to
  write &lt;code&gt;lit(",")&lt;/code&gt; or &lt;code&gt;lit(2)&lt;/code&gt; when the natural Python form would be
  &lt;code&gt;","&lt;/code&gt; or &lt;code&gt;2&lt;/code&gt;, and apply the fix.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;audit-skill-md&lt;/code&gt;&lt;/strong&gt; — &lt;em&gt;the user-facing skill has drifted from the
  API it documents.&lt;/em&gt; After new APIs are added or old ones renamed, this
  skill walks the public surface and flags every place where
  &lt;code&gt;SKILL.md&lt;/code&gt; is now stale.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In practice the same person — whoever is driving the upstream sync —
will often invoke all three in sequence as part of the same chore. The
&lt;a href="https://github.com/apache/datafusion-python/blob/main/dev/release/upstream-sync.md"&gt;upstream-sync runbook&lt;/a&gt; in the repo walks through exactly that: bump
the dependency, then run &lt;code&gt;check-upstream&lt;/code&gt;, then optionally
&lt;code&gt;make-pythonic&lt;/code&gt; on anything newly exposed, then &lt;code&gt;audit-skill-md&lt;/code&gt; to
catch any user-skill drift the new APIs introduced. They are still kept
as three separate skills rather than one mega-skill because each has a
distinct trigger, a distinct success criterion, and a distinct kind of
output (issues, signature edits, doc edits). Bundling them would
collapse those into a single sprawling prompt and make it harder to
tell whether the current step has actually finished.&lt;/p&gt;
&lt;p&gt;The rest of this section walks through each one in turn — how it
works, what we learned writing it, and (for &lt;code&gt;check-upstream&lt;/code&gt; and
&lt;code&gt;make-pythonic&lt;/code&gt;) how the first runs immediately surfaced gaps in the
skill itself that became the next round of edits.&lt;/p&gt;
&lt;h3 id="check-upstream-find-missing-bindings"&gt;&lt;code&gt;check-upstream&lt;/code&gt;: Find Missing Bindings&lt;a class="headerlink" href="#check-upstream-find-missing-bindings" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;datafusion-python&lt;/code&gt; is a thin Python binding over the Rust &lt;a href="https://datafusion.apache.org"&gt;Apache
DataFusion&lt;/a&gt; library. Every release of upstream DataFusion adds new
functions, methods, and types, and one of the most common forms of
maintenance drift is &lt;em&gt;failing to expose those additions in Python&lt;/em&gt;. The
project would happily ship a release where, for example, &lt;code&gt;array_transform&lt;/code&gt;
was available in DataFusion but missing from &lt;code&gt;datafusion.functions&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;The &lt;a href="https://github.com/apache/datafusion-python/blob/main/.ai/skills/check-upstream/SKILL.md"&gt;&lt;code&gt;check-upstream&lt;/code&gt;&lt;/a&gt; skill is a structured audit. The
agent walks the upstream surface — scalar functions, aggregate functions,
window functions, DataFrame methods, SessionContext methods, FFI types —
compares each against the Python API, and emits a report of what's
missing.&lt;/p&gt;
&lt;p&gt;We added the skill in &lt;a href="https://github.com/apache/datafusion-python/pull/1460"&gt;PR #1460&lt;/a&gt; and immediately used it to generate twelve
GitHub issues (&lt;a href="https://github.com/apache/datafusion-python/issues/1448"&gt;#1448&lt;/a&gt; – &lt;a href="https://github.com/apache/datafusion-python/issues/1459"&gt;#1459&lt;/a&gt;), one per gap. That batch
of issues is what made the skill useful: each one was a concrete,
verifiable claim that some upstream feature wasn't exposed.&lt;/p&gt;
&lt;p&gt;It was also the first place we hit the &lt;strong&gt;iterative-update pattern&lt;/strong&gt; that
became core to how we maintain these skills.&lt;/p&gt;
&lt;h3 id="skills-are-software-they-need-a-feedback-loop"&gt;Skills Are Software: They Need a Feedback Loop&lt;a class="headerlink" href="#skills-are-software-they-need-a-feedback-loop" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;When we ran &lt;code&gt;check-upstream&lt;/code&gt; for the first time and started working through
the twelve generated issues, several of them were wrong in subtle ways.
Some reported a function as missing when it was actually present under an
alias. Some missed the fact that the Python layer can implement an
"upstream" function by calling a different underlying Rust binding — the
agent had assumed a 1:1 correspondence between Rust &lt;code&gt;#[pyfunction]&lt;/code&gt;
declarations and Python coverage. Some missed the distinction between
"this entire major release added a function" and "this patch release fixed
bugs only, so nothing to find" — the agent stopped looking after seeing a
quiet changelog.&lt;/p&gt;
&lt;p&gt;We did not throw away the issues. We walked through them one by one and,
for each false positive, asked: &lt;em&gt;what would the skill have to say for the
agent to not make this mistake?&lt;/em&gt; Then we changed the skill.&lt;/p&gt;
&lt;p&gt;Three of those updates are worth quoting because they capture the kind of
guidance an agent will not infer on its own:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;The Python API is the source of truth for coverage.&lt;/strong&gt; A function is
considered "exposed" if it exists in the Python API, even if there is no
corresponding entry in the Rust bindings. Many upstream functions are
aliases ... do NOT report a function as missing if it appears in the
Python &lt;code&gt;__all__&lt;/code&gt; list and has a working implementation.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Audit the total upstream surface, not the delta since the last pin.&lt;/strong&gt;
Gaps accumulate across syncs. A patch-release bump with a "bug fixes
only" changelog does not mean there is nothing to find — pre-existing
gaps from earlier majors still need to be surfaced.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;The third addition was a table of &lt;strong&gt;compile-signal triggers&lt;/strong&gt;: patterns
that show up when you fix the compile errors during an upstream bump,
mapped to the class of binding gap they imply. For example: a new
&lt;code&gt;Expr::*&lt;/code&gt; variant added to a non-exhaustive &lt;code&gt;match&lt;/code&gt; means a new family of
lambda or higher-order scalar functions has appeared upstream; a new
&lt;code&gt;ScalarValue::*&lt;/code&gt; variant means new array functions that produce or consume
the type. We learned each of these the hard way by missing them during a
sync, then encoded them so the next sync wouldn't.&lt;/p&gt;
&lt;p&gt;The point is not the specific rules. The point is the &lt;em&gt;mechanism&lt;/em&gt;: every
time the skill gets something wrong in the real world, that wrongness
gets converted into a rule the skill emits next time.&lt;/p&gt;
&lt;h3 id="make-pythonic-fix-the-ergonomics"&gt;&lt;code&gt;make-pythonic&lt;/code&gt;: Fix the Ergonomics&lt;a class="headerlink" href="#make-pythonic-fix-the-ergonomics" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The second developer skill, &lt;a href="https://github.com/apache/datafusion-python/blob/main/.ai/skills/make-pythonic/SKILL.md"&gt;&lt;code&gt;make-pythonic&lt;/code&gt;&lt;/a&gt;, improves the
Python API's ergonomics. Many functions historically required explicit
&lt;code&gt;lit()&lt;/code&gt; wrapping for arguments that are contextually always literal: you
had to write &lt;code&gt;split_part(col("a"), lit(","), lit(2))&lt;/code&gt; when the natural
Python form was &lt;code&gt;split_part(col("a"), ",", 2)&lt;/code&gt;. The skill audits each
function in &lt;code&gt;python/datafusion/functions.py&lt;/code&gt;, categorizes its arguments,
and updates type hints and coercion logic to accept native Python types
where it is safe to do so.&lt;/p&gt;
&lt;p&gt;We landed it in &lt;a href="https://github.com/apache/datafusion-python/pull/1484"&gt;PR #1484&lt;/a&gt; alongside the actual ergonomic improvements it
generated — 47 functions across date/time, string, regex, math, and array
families.&lt;/p&gt;
&lt;p&gt;That PR is also useful as a case study for &lt;em&gt;how to design a skill in the
first place&lt;/em&gt;, because it includes the &lt;a href="https://github.com/user-attachments/files/26608305/chat-export-2026-04-09.md"&gt;full transcript&lt;/a&gt; of
the conversation in which the skill was built. A few findings from that
transcript are worth pulling out:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;1. The skill grew out of a conversation, not a spec.&lt;/strong&gt;
The first prompt was a paragraph describing the problem in plain language:
"there are places where inputting multiple types of data as function
arguments should just work as opposed to the Rust versions." The agent
explored the codebase, identified ten concrete examples of non-Pythonic
signatures, and drafted the skill. Subsequent prompts (&lt;em&gt;"how do you tell
if upstream only accepts a literal?"&lt;/em&gt;) pulled in the &lt;strong&gt;second signal&lt;/strong&gt; —
inspecting the Rust &lt;code&gt;invoke_with_args()&lt;/code&gt; and &lt;code&gt;Signature::coercible()&lt;/code&gt;
implementations — which became a section in the skill.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;2. Designing and testing happen in separate sessions.&lt;/strong&gt;
After the skill was drafted, the author explicitly exited the session and
started a fresh one to test it. The reason is the same one that drove the
fresh-session rule in the TPC-H evaluation: the skill has to be evaluated
on what &lt;em&gt;it&lt;/em&gt; contains, not on what the agent and the author worked out
together in the design conversation. Prior context is contamination.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;3. The first test run found a real bug — in the skill, not the code.&lt;/strong&gt;
The initial draft put &lt;code&gt;date_part&lt;/code&gt;'s &lt;code&gt;part&lt;/code&gt; argument into &lt;strong&gt;Category B&lt;/strong&gt;
(native type only) because the upstream Rust enforces a non-null scalar
&lt;code&gt;Utf8&lt;/code&gt;. The test suite immediately failed: an existing test passed
&lt;code&gt;lit("month")&lt;/code&gt;, and &lt;code&gt;lit()&lt;/code&gt; produces an &lt;code&gt;Expr&lt;/code&gt;. The fix was not to change
the test — it was to relax the category. &lt;code&gt;date_part&lt;/code&gt; moved to &lt;strong&gt;Category
A&lt;/strong&gt; (&lt;code&gt;Expr | str&lt;/code&gt;), and the skill grew a note that "literal-only at the
Rust layer" is not the same as "rejects an &lt;code&gt;Expr&lt;/code&gt; at the Python layer." A
real test that exercises the change is what surfaced this; the skill
alone would not have.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;4. Reviewing the agent's work found gaps the skill didn't cover.&lt;/strong&gt;
After the first commit landed, a single follow-up question — &lt;em&gt;"were
there any functions that were aliases to the functions you updated that
should likewise have their signatures changed?"&lt;/em&gt; — surfaced two missed
functions: &lt;code&gt;instr&lt;/code&gt; and &lt;code&gt;position&lt;/code&gt;, both aliases of &lt;code&gt;strpos&lt;/code&gt;. The skill
had been silent on aliases. We fixed the two signatures &lt;em&gt;and&lt;/em&gt; added a
new Step 3 ("Update Alias Type Hints") to the skill, so the next person
to run it wouldn't have to ask the same question.&lt;/p&gt;
&lt;p&gt;This is the same pattern as the &lt;code&gt;check-upstream&lt;/code&gt; story: an issue surfaces
in review, gets converted into a rule, the rule lives in the skill.&lt;/p&gt;
&lt;h3 id="audit-skill-md-keep-the-user-skill-up-to-date"&gt;&lt;code&gt;audit-skill-md&lt;/code&gt;: Keep the User Skill Up to Date&lt;a class="headerlink" href="#audit-skill-md-keep-the-user-skill-up-to-date" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The third developer skill closes the loop. The user skill at
&lt;code&gt;skills/datafusion_python/SKILL.md&lt;/code&gt; documents the public Python API —
which means every time the public Python API changes, the user skill is
at risk of becoming stale. New functions need to be documented. Renamed
or removed APIs need to be scrubbed. Examples that used to be idiomatic
may have drifted as the library added better patterns.&lt;/p&gt;
&lt;p&gt;&lt;a href="https://github.com/apache/datafusion-python/blob/main/.ai/skills/audit-skill-md/SKILL.md"&gt;&lt;code&gt;audit-skill-md&lt;/code&gt;&lt;/a&gt; is the skill that audits the &lt;em&gt;other&lt;/em&gt;
skill. It walks the public surface of &lt;code&gt;SessionContext&lt;/code&gt;, &lt;code&gt;DataFrame&lt;/code&gt;,
&lt;code&gt;Expr&lt;/code&gt;, and &lt;code&gt;functions&lt;/code&gt;, cross-references each against the contents of
&lt;code&gt;SKILL.md&lt;/code&gt;, and flags drift. It is meant to be run right after the
&lt;code&gt;check-upstream&lt;/code&gt; step of an upstream sync: once any new APIs are exposed,
this skill makes sure they get documented.&lt;/p&gt;
&lt;p&gt;The three developer skills form a small pipeline:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;upstream DataFusion release
        │
        ▼
  check-upstream  ──►  issues filed for missing bindings
        │
        ▼
   bindings landed
        │
        ▼
  make-pythonic   ──►  ergonomic cleanups on the new surface
        │
        ▼
  audit-skill-md  ──►  user skill updated to teach the new surface
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Each step has a skill; each skill produces concrete artifacts (issues,
PRs, doc edits); and each step's output is the next step's input.&lt;/p&gt;
&lt;h2 id="lessons-that-generalize"&gt;Lessons That Generalize&lt;a class="headerlink" href="#lessons-that-generalize" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;If you take one thing from the DataFusion Python experience, take this:
&lt;strong&gt;a skill is software, and like all software it needs a feedback loop.&lt;/strong&gt;
The first version of a skill is always wrong. It is wrong in ways you will
not predict by re-reading it; you will only discover the gaps by running
it and watching what the agent does. The skill becomes good only by being
edited every time you catch it failing.&lt;/p&gt;
&lt;p&gt;Some more specific lessons:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Pick your audience before you write a line.&lt;/strong&gt; A skill for users and a
  skill for maintainers are different documents. If you can't decide who
  it's for, you'll write something that helps neither.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pay attention to where the file lives.&lt;/strong&gt; Public skills go where the
  skill ecosystem expects to find them, in a small subtree the tooling
  can fetch without pulling the whole repo. Internal skills live wherever
  is convenient for contributors.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Find a corpus that's adversarial to your own training data.&lt;/strong&gt; TPC-H
  worked for us because it has English problem statements, machine-checkable
  answers, and a thousand SQL implementations on the public web that we
  explicitly tell the agent to ignore. The "ignore" rule is what makes the
  evaluation honest.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Use fresh sessions for evaluation.&lt;/strong&gt; Prior conversation is leakage.
  If the agent already knows the answer from designing the skill with
  you, it can't tell you whether the skill itself works.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Treat every bad output as a skill update.&lt;/strong&gt; When you find the agent
  doing the wrong thing — in CI, in code review, in a generated issue —
  the question to ask is not "how do I fix this PR?" It is "what would
  the skill have to say so the next run doesn't make this mistake?"&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The skills in &lt;code&gt;datafusion-python&lt;/code&gt; are not finished, and they will
not be finished. Each upstream sync surfaces new gaps. Each review of
agent-generated code surfaces new pitfalls to encode. Each new abstraction
the project adds is one more thing the user skill needs to teach. That is
fine — the feedback loop &lt;em&gt;is&lt;/em&gt; the work. The skills you ship today are the
starting point for the skills you'll ship next quarter.&lt;/p&gt;
&lt;p&gt;If you maintain an open source project of any complexity and your users
are starting to ask agents to use it, this is a pattern worth stealing.
Start with one skill for the people who use your library. Add another for
the people who maintain it. Find a corpus you can use to test the first
one. Then keep editing.&lt;/p&gt;
&lt;h2 id="acknowledgements"&gt;Acknowledgements&lt;a class="headerlink" href="#acknowledgements" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;Thanks to &lt;a href="https://github.com/alamb"&gt;@alamb&lt;/a&gt;, &lt;a href="https://github.com/kevinjqliu"&gt;@kevinjqliu&lt;/a&gt;, &lt;a href="https://github.com/ntjohnson1"&gt;@ntjohnson1&lt;/a&gt;, and &lt;a href="https://github.com/xudong963"&gt;@xudong963&lt;/a&gt; for
their contributions and discussion on the skills and the PRs and issues
referenced in this post.&lt;/p&gt;
&lt;p&gt;The skills themselves were drafted in collaboration with Claude, in the
spirit described above — agents are well suited to writing for other
agents, provided a maintainer is there to supply the project-specific
knowledge they cannot infer.&lt;/p&gt;
&lt;h2 id="get-involved"&gt;Get Involved&lt;a class="headerlink" href="#get-involved" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The DataFusion team is an active and engaging community and we would love
to have you join us and help the project.&lt;/p&gt;
&lt;p&gt;Here are some ways to get involved:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Learn more by visiting the &lt;a href="https://datafusion.apache.org/index.html"&gt;DataFusion&lt;/a&gt; project page.&lt;/li&gt;
&lt;li&gt;Try out the project and provide feedback, file issues, and contribute code.&lt;/li&gt;
&lt;li&gt;Work on a &lt;a href="https://github.com/apache/datafusion-python/issues?q=is%3Aissue+is%3Aopen+label%3A%22good+first+issue%22"&gt;good first issue&lt;/a&gt;.&lt;/li&gt;
&lt;li&gt;Reach out to us via the &lt;a href="https://datafusion.apache.org/contributor-guide/communication.html"&gt;communication doc&lt;/a&gt;.&lt;/li&gt;
&lt;/ul&gt;</content><category term="blog"/></entry><entry><title>Writing Custom Table Providers in Apache DataFusion</title><link href="https://datafusion.apache.org/blog/2026/03/31/writing-table-providers" rel="alternate"/><published>2026-03-31T00:00:00+00:00</published><updated>2026-03-31T00:00:00+00:00</updated><author><name>Tim Saucer (rerun.io)</name></author><id>tag:datafusion.apache.org,2026-03-31:/blog/2026/03/31/writing-table-providers</id><summary type="html">&lt;!--
{% comment %}
Licensed to the Apache Software Foundation (ASF) under one or more
contributor license agreements.  See the NOTICE file distributed with
this work for additional information regarding copyright ownership.
The ASF licenses this file to you under the Apache License, Version 2.0
(the "License"); you may not use this file except in compliance with
the License.  You may obtain a copy of the License at

http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
{% endcomment %}
--&gt;

&lt;p&gt;One of DataFusion's greatest strengths is its extensibility. If your data lives
in a custom format, behind an API, or in a system that DataFusion does not
natively support, you can teach DataFusion to read it by implementing a
&lt;strong&gt;custom table provider&lt;/strong&gt;. This post walks through the three layers you …&lt;/p&gt;</summary><content type="html">&lt;!--
{% comment %}
Licensed to the Apache Software Foundation (ASF) under one or more
contributor license agreements.  See the NOTICE file distributed with
this work for additional information regarding copyright ownership.
The ASF licenses this file to you under the Apache License, Version 2.0
(the "License"); you may not use this file except in compliance with
the License.  You may obtain a copy of the License at

http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
{% endcomment %}
--&gt;

&lt;p&gt;One of DataFusion's greatest strengths is its extensibility. If your data lives
in a custom format, behind an API, or in a system that DataFusion does not
natively support, you can teach DataFusion to read it by implementing a
&lt;strong&gt;custom table provider&lt;/strong&gt;. This post walks through the three layers you need to
understand to design a table provider and where planning and execution work should happen.&lt;/p&gt;
&lt;h2 id="the-three-layers"&gt;The Three Layers&lt;a class="headerlink" href="#the-three-layers" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;When DataFusion executes a query against a table, three abstractions collaborate
to produce results:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/catalog/trait.TableProvider.html"&gt;TableProvider&lt;/a&gt;&lt;/strong&gt; -- Describes the table (schema, capabilities) and
   produces an execution plan when queried. This is part of the &lt;strong&gt;Logical Plan&lt;/strong&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/physical_plan/trait.ExecutionPlan.html"&gt;ExecutionPlan&lt;/a&gt;&lt;/strong&gt; -- Describes &lt;em&gt;how&lt;/em&gt; to compute the result: partitioning,
   ordering, and child plan relationships. This is part of the &lt;strong&gt;Physical Plan&lt;/strong&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/execution/type.SendableRecordBatchStream.html"&gt;SendableRecordBatchStream&lt;/a&gt;&lt;/strong&gt; -- The async stream that &lt;em&gt;actually does the
   work&lt;/em&gt;, yielding &lt;code&gt;RecordBatch&lt;/code&gt;es one at a time.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Think of these as a funnel: &lt;code&gt;TableProvider::scan()&lt;/code&gt; is called once during
planning to create an &lt;code&gt;ExecutionPlan&lt;/code&gt;, then &lt;code&gt;ExecutionPlan::execute()&lt;/code&gt; is called
once per partition to create a stream, and those streams are where rows are
actually produced during execution.&lt;/p&gt;
&lt;h2 id="background-logical-and-physical-planning"&gt;Background: Logical and Physical Planning&lt;a class="headerlink" href="#background-logical-and-physical-planning" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;Before diving into the three layers, it helps to understand how DataFusion
processes a query. There are several phases between a SQL string (or DataFrame
call) and streaming results:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-text"&gt;SQL / DataFrame API
  → Logical Plan          (abstract: what to compute)
  → Logical Optimization  (rewrite rules that preserve semantics)
  → Physical Plan         (concrete: how to compute it)
  → Physical Optimization (hardware- and data-aware rewrites)
  → Execution             (streaming RecordBatches)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id="logical-planning"&gt;Logical Planning&lt;a class="headerlink" href="#logical-planning" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;A &lt;strong&gt;logical plan&lt;/strong&gt; describes &lt;em&gt;what&lt;/em&gt; the query computes without specifying &lt;em&gt;how&lt;/em&gt;.
It is a tree of relational operators -- &lt;code&gt;TableScan&lt;/code&gt;, &lt;code&gt;Filter&lt;/code&gt;, &lt;code&gt;Projection&lt;/code&gt;,
&lt;code&gt;Aggregate&lt;/code&gt;, &lt;code&gt;Join&lt;/code&gt;, &lt;code&gt;Sort&lt;/code&gt;, &lt;code&gt;Limit&lt;/code&gt;, and so on. The logical optimizer rewrites
this tree to reduce work while preserving the query's meaning. Some logical
optimizations include:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Predicate pushdown&lt;/strong&gt; -- moves filters as close to the data source as
  possible, so fewer rows flow through the rest of the plan.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Projection pruning&lt;/strong&gt; -- eliminates columns that are never referenced
  downstream, reducing memory and I/O.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Expression simplification&lt;/strong&gt; -- rewrites expressions like &lt;code&gt;1 = 1&lt;/code&gt; or
  &lt;code&gt;x AND true&lt;/code&gt; into simpler forms.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Subquery decorrelation&lt;/strong&gt; -- converts correlated &lt;code&gt;IN&lt;/code&gt; / &lt;code&gt;EXISTS&lt;/code&gt; subqueries
  into more efficient semi-joins.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Limit pushdown&lt;/strong&gt; -- pushes &lt;code&gt;LIMIT&lt;/code&gt; earlier in the plan so operators
  produce less data.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="physical-planning"&gt;Physical Planning&lt;a class="headerlink" href="#physical-planning" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The &lt;strong&gt;physical planner&lt;/strong&gt; converts the optimized logical plan into an
&lt;code&gt;ExecutionPlan&lt;/code&gt; tree -- the concrete plan that will actually run. This is where
decisions like "use a hash join vs. a sort-merge join" or "how many partitions
to scan" are made. The physical optimizer then refines this tree further with rewrites such as:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Distribution enforcement&lt;/strong&gt; -- inserts &lt;code&gt;RepartitionExec&lt;/code&gt; nodes so that data
  is partitioned correctly for joins and aggregations.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Sort enforcement&lt;/strong&gt; -- inserts &lt;code&gt;SortExec&lt;/code&gt; nodes where ordering is required,
  and removes them where the data is already sorted.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Join selection&lt;/strong&gt; -- picks the most efficient join strategy based on
  statistics and table sizes.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Aggregate optimization&lt;/strong&gt; -- combines partial and final aggregation stages,
  and can use exact statistics to skip scanning entirely.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="why-this-matters-for-table-providers"&gt;Why This Matters for Table Providers&lt;a class="headerlink" href="#why-this-matters-for-table-providers" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Your &lt;code&gt;TableProvider&lt;/code&gt; sits at the boundary between logical and physical planning.
During logical optimization, DataFusion determines which filters and projections
&lt;em&gt;could&lt;/em&gt; be pushed down to the source. When &lt;code&gt;scan()&lt;/code&gt; is called during physical
planning, those hints are passed to you. By implementing capabilities like
&lt;code&gt;supports_filters_pushdown&lt;/code&gt;, you influence what the optimizer can do -- and the
metadata you declare in your &lt;code&gt;ExecutionPlan&lt;/code&gt; (partitioning, ordering) directly
affects which physical optimizations apply.&lt;/p&gt;
&lt;h2 id="choosing-the-right-starting-point"&gt;Choosing the Right Starting Point&lt;a class="headerlink" href="#choosing-the-right-starting-point" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;Not every custom data source requires implementing all three layers from
scratch. DataFusion provides building blocks that let you plug in at whatever
level makes sense:&lt;/p&gt;
&lt;table class="table"&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;If your data is...&lt;/th&gt;
&lt;th&gt;Start with&lt;/th&gt;
&lt;th&gt;You implement&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Already in &lt;code&gt;RecordBatch&lt;/code&gt;es in memory&lt;/td&gt;
&lt;td&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/memory/struct.MemTable.html"&gt;MemTable&lt;/a&gt;&lt;/td&gt;
&lt;td&gt;Nothing -- just construct it&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;An async stream of batches&lt;/td&gt;
&lt;td&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/stream/struct.StreamTable.html"&gt;StreamTable&lt;/a&gt;&lt;/td&gt;
&lt;td&gt;A stream factory&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;A logical transformation of other tables&lt;/td&gt;
&lt;td&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/view/struct.ViewTable.html"&gt;ViewTable&lt;/a&gt; wrapping a logical plan&lt;/td&gt;
&lt;td&gt;The logical plan&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;A variant of an existing file format&lt;/td&gt;
&lt;td&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/listing/struct.ListingTable.html"&gt;ListingTable&lt;/a&gt; with a custom &lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/file_format/trait.FileFormat.html"&gt;FileFormat&lt;/a&gt; wrapping an existing one&lt;/td&gt;
&lt;td&gt;A thin &lt;code&gt;FileFormat&lt;/code&gt; wrapper&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Files in a custom format on disk or object storage&lt;/td&gt;
&lt;td&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/listing/struct.ListingTable.html"&gt;ListingTable&lt;/a&gt; with a custom &lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/file_format/trait.FileFormat.html"&gt;FileFormat&lt;/a&gt;, &lt;a href="https://docs.rs/datafusion-datasource/latest/datafusion_datasource/file/trait.FileSource.html"&gt;FileSource&lt;/a&gt;, and &lt;a href="https://docs.rs/datafusion-datasource/latest/datafusion_datasource/file_stream/trait.FileOpener.html"&gt;FileOpener&lt;/a&gt;&lt;/td&gt;
&lt;td&gt;The format, source, and opener&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;A custom source needing full control&lt;/td&gt;
&lt;td&gt;&lt;code&gt;TableProvider&lt;/code&gt; + &lt;code&gt;ExecutionPlan&lt;/code&gt; + stream&lt;/td&gt;
&lt;td&gt;All three layers&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;If your data is file-based, &lt;code&gt;ListingTable&lt;/code&gt; handles file discovery, partition
column inference, and plan construction -- you only need to implement
&lt;code&gt;FileFormat&lt;/code&gt;, &lt;code&gt;FileSource&lt;/code&gt;, and &lt;code&gt;FileOpener&lt;/code&gt; to describe how to read your
files. See the &lt;a href="https://github.com/apache/datafusion/blob/main/datafusion-examples/examples/custom_data_source/custom_file_format.rs"&gt;custom_file_format example&lt;/a&gt; for a minimal wrapping approach,
or &lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/file_format/parquet/struct.ParquetSource.html"&gt;ParquetSource&lt;/a&gt; and &lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/file_format/parquet/struct.ParquetOpener.html"&gt;ParquetOpener&lt;/a&gt; for a full custom implementation to
use as a reference.&lt;/p&gt;
&lt;p&gt;The rest of this post focuses on the full &lt;code&gt;TableProvider&lt;/code&gt; + &lt;code&gt;ExecutionPlan&lt;/code&gt; +
stream path, which gives you complete control and applies to any data source.&lt;/p&gt;
&lt;h2 id="layer-1-tableprovider"&gt;Layer 1: TableProvider&lt;a class="headerlink" href="#layer-1-tableprovider" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;A &lt;a href="https://docs.rs/datafusion/latest/datafusion/catalog/trait.TableProvider.html"&gt;TableProvider&lt;/a&gt; represents a queryable data source. For a minimal read-only
table, you need four methods:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-rust"&gt;impl TableProvider for MyTable {
    fn as_any(&amp;amp;self) -&amp;gt; &amp;amp;dyn Any { self }

    fn schema(&amp;amp;self) -&amp;gt; SchemaRef {
        Arc::clone(&amp;amp;self.schema)
    }

    fn table_type(&amp;amp;self) -&amp;gt; TableType {
        TableType::Base
    }

    async fn scan(
        &amp;amp;self,
        state: &amp;amp;dyn Session,
        projection: Option&amp;lt;&amp;amp;Vec&amp;lt;usize&amp;gt;&amp;gt;,
        filters: &amp;amp;[Expr],
        limit: Option&amp;lt;usize&amp;gt;,
    ) -&amp;gt; Result&amp;lt;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt; {
        // Build and return an ExecutionPlan -- don't do any execution work here -- keep lightweight!
        Ok(Arc::new(MyExecPlan::new(
            Arc::clone(&amp;amp;self.schema),
            projection,
            limit,
        )))
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;scan&lt;/code&gt; method is the heart of &lt;code&gt;TableProvider&lt;/code&gt;. It receives three pushdown
hints from the optimizer, each reducing the amount of data your source needs
to produce:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;projection&lt;/code&gt;&lt;/strong&gt; -- Which columns are needed. This reduces the &lt;strong&gt;width&lt;/strong&gt; of
  the output. If your source supports it, read only these columns rather than
  the full schema.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;filters&lt;/code&gt;&lt;/strong&gt; -- Predicates the engine would like you to apply during the
  scan. This reduces the &lt;strong&gt;number of rows&lt;/strong&gt; by skipping data that does not
  match. Implement &lt;code&gt;supports_filters_pushdown&lt;/code&gt; to advertise which filters you
  can handle.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;limit&lt;/code&gt;&lt;/strong&gt; -- A row count cap. This also reduces the &lt;strong&gt;number of rows&lt;/strong&gt; --
  if you can stop reading early once you have produced enough rows, this avoids
  unnecessary work.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;You can also use the &lt;a href="https://docs.rs/datafusion/latest/datafusion/catalog/trait.TableProvider.html#method.scan_with_args"&gt;scan_with_args()&lt;/a&gt;
variant that provides additional pushdown information for other advanced use cases.&lt;/p&gt;
&lt;h3 id="keep-scan-lightweight"&gt;Keep &lt;code&gt;scan()&lt;/code&gt; Lightweight&lt;a class="headerlink" href="#keep-scan-lightweight" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;This is a critical point: &lt;strong&gt;&lt;code&gt;scan()&lt;/code&gt; runs during planning, not execution.&lt;/strong&gt; It
should return quickly. Best practice is to avoid performing I/O, network
calls, or heavy computation here. The &lt;code&gt;scan&lt;/code&gt; method's job is to &lt;em&gt;describe&lt;/em&gt; how
the data will be produced, not to produce it. All the real work belongs in the
stream (Layer 3).&lt;/p&gt;
&lt;p&gt;A common pitfall is to fetch data or open connections in &lt;code&gt;scan()&lt;/code&gt;. This blocks
the planning thread and can cause timeouts or deadlocks, especially if the query
involves multiple tables or subqueries that all need to be planned before
execution begins.&lt;/p&gt;
&lt;h3 id="existing-implementations-to-learn-from"&gt;Existing Implementations to Learn From&lt;a class="headerlink" href="#existing-implementations-to-learn-from" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;DataFusion ships several &lt;code&gt;TableProvider&lt;/code&gt; implementations that are excellent
references:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/memory/struct.MemTable.html"&gt;MemTable&lt;/a&gt;&lt;/strong&gt; -- Holds data in memory as &lt;code&gt;Vec&amp;lt;RecordBatch&amp;gt;&lt;/code&gt;. The simplest
  possible provider; great for tests and small datasets.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/stream/struct.StreamTable.html"&gt;StreamTable&lt;/a&gt;&lt;/strong&gt; -- Wraps a user-provided stream factory. Useful when your
  data arrives as a continuous stream (e.g., from Kafka or a socket).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/listing/struct.ListingTable.html"&gt;ListingTable&lt;/a&gt;&lt;/strong&gt; -- The file-based data source behind DataFusion's
  built-in Parquet, CSV, and JSON support. Demonstrates sophisticated filter
  and projection pushdown, file pruning, and schema inference.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/view/struct.ViewTable.html"&gt;ViewTable&lt;/a&gt;&lt;/strong&gt; -- Wraps a logical plan, representing a SQL view. Useful
  if your provider is best expressed as a transformation of other tables.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="layer-2-executionplan"&gt;Layer 2: ExecutionPlan&lt;a class="headerlink" href="#layer-2-executionplan" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;An &lt;a href="https://docs.rs/datafusion/latest/datafusion/physical_plan/trait.ExecutionPlan.html"&gt;ExecutionPlan&lt;/a&gt; is a node in the physical query plan tree. Your table
provider's &lt;code&gt;scan()&lt;/code&gt; method returns one. The required methods are:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-rust"&gt;impl ExecutionPlan for MyExecPlan {
    fn name(&amp;amp;self) -&amp;gt; &amp;amp;str { "MyExecPlan" }

    fn as_any(&amp;amp;self) -&amp;gt; &amp;amp;dyn Any { self }

    fn properties(&amp;amp;self) -&amp;gt; &amp;amp;PlanProperties {
        &amp;amp;self.properties
    }

    fn children(&amp;amp;self) -&amp;gt; Vec&amp;lt;&amp;amp;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt; {
        vec![]  // Leaf node -- no children
    }

    fn with_new_children(
        self: Arc&amp;lt;Self&amp;gt;,
        children: Vec&amp;lt;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt;,
    ) -&amp;gt; Result&amp;lt;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt; {
        assert!(children.is_empty());
        Ok(self)
    }

    fn execute(
        &amp;amp;self,
        partition: usize,
        context: Arc&amp;lt;TaskContext&amp;gt;,
    ) -&amp;gt; Result&amp;lt;SendableRecordBatchStream&amp;gt; {
        // This is where you build and return your stream
        // ...
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The key properties to set correctly in &lt;a href="https://docs.rs/datafusion/latest/datafusion/physical_plan/struct.PlanProperties.html"&gt;PlanProperties&lt;/a&gt; are &lt;strong&gt;output
partitioning&lt;/strong&gt; and &lt;strong&gt;output ordering&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Output partitioning&lt;/strong&gt; tells the engine how many partitions your data has,
which determines parallelism. If your source naturally partitions data (e.g.,
by file or by shard), expose that here.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Output ordering&lt;/strong&gt; declares whether your data is naturally sorted. This
enables the optimizer to avoid inserting a &lt;code&gt;SortExec&lt;/code&gt; when a query requires
ordered data. Getting this right can be a significant performance win.&lt;/p&gt;
&lt;h3 id="partitioning-strategies"&gt;Partitioning Strategies&lt;a class="headerlink" href="#partitioning-strategies" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Since &lt;code&gt;execute()&lt;/code&gt; is called once per partition, partitioning directly controls
the parallelism of your table scan. Each partition produces an independent
stream that DataFusion schedules as a &lt;strong&gt;task&lt;/strong&gt; on the tokio runtime. It is
important to distinguish tasks from threads: tasks are lightweight units of
async work that are multiplexed onto a thread pool. You can have many more
tasks (partitions) than physical threads -- the runtime will interleave them
efficiently as they await I/O or yield.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Start simple: match your data's natural layout.&lt;/strong&gt; If you have 4 files, expose
4 partitions. If your source has 8 shards, expose 8 partitions. DataFusion will
insert a &lt;code&gt;RepartitionExec&lt;/code&gt; above your scan when downstream operators need a
different distribution. You can also implement the
&lt;a href="https://docs.rs/datafusion/latest/datafusion/physical_plan/trait.ExecutionPlan.html#method.repartitioned"&gt;&lt;code&gt;repartitioned&lt;/code&gt;&lt;/a&gt;
method on your &lt;code&gt;ExecutionPlan&lt;/code&gt; to let DataFusion request a different partition
count directly from your source, avoiding the extra operator entirely.&lt;/p&gt;
&lt;p&gt;Consider how your data source naturally divides its data:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;By file or object:&lt;/strong&gt; If you are reading from S3, each file can be a
  partition. DataFusion will read them in parallel.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;By shard or region:&lt;/strong&gt; If your source is a sharded database, each shard
  maps naturally to a partition.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;By key range:&lt;/strong&gt; If your data is keyed (e.g., by timestamp or customer ID),
  you can split it into ranges.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Advanced: aligning with &lt;code&gt;target_partitions&lt;/code&gt;.&lt;/strong&gt; Once you have something
working, you can tune further. Having &lt;em&gt;too many&lt;/em&gt; partitions is not free: each
partition adds scheduling overhead, and downstream operators may need to
repartition the data anyway. The session configuration exposes a
&lt;strong&gt;target partition count&lt;/strong&gt; that reflects how many partitions the optimizer
expects to work with:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-rust"&gt;async fn scan(
    &amp;amp;self,
    state: &amp;amp;dyn Session,
    projection: Option&amp;lt;&amp;amp;Vec&amp;lt;usize&amp;gt;&amp;gt;,
    filters: &amp;amp;[Expr],
    limit: Option&amp;lt;usize&amp;gt;,
) -&amp;gt; Result&amp;lt;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt; {
    let target_partitions = state.config().target_partitions();
    // Optionally coalesce or split partitions to match target_partitions.
    // ...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If your source produces data in exactly &lt;code&gt;target_partitions&lt;/code&gt; partitions, the
optimizer is less likely to insert a &lt;code&gt;RepartitionExec&lt;/code&gt; above your scan.
For small datasets, &lt;code&gt;target_partitions&lt;/code&gt; may be set to 1, which avoids any
repartitioning overhead entirely.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Advanced: declaring hash partitioning.&lt;/strong&gt; If your source stores data
pre-partitioned by a specific key (e.g., &lt;code&gt;customer_id&lt;/code&gt;), you can declare this
in your output partitioning. For a query like:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-sql"&gt;SELECT customer_id, SUM(amount)
FROM my_table
GROUP BY customer_id;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If you declare your output partitioning as &lt;code&gt;Hash([customer_id], N)&lt;/code&gt;, the
optimizer recognizes that the data is already distributed correctly for the
aggregation and eliminates the &lt;code&gt;RepartitionExec&lt;/code&gt; that would otherwise appear
in the plan. You can verify this with &lt;code&gt;EXPLAIN&lt;/code&gt; (more on this below).&lt;/p&gt;
&lt;p&gt;Conversely, if you report &lt;code&gt;UnknownPartitioning&lt;/code&gt;, DataFusion must assume the
worst case and will always insert repartitioning operators as needed.&lt;/p&gt;
&lt;h3 id="keep-execute-lightweight-too"&gt;Keep &lt;code&gt;execute()&lt;/code&gt; Lightweight Too&lt;a class="headerlink" href="#keep-execute-lightweight-too" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Like &lt;code&gt;scan()&lt;/code&gt;, the &lt;code&gt;execute()&lt;/code&gt; method should construct and return a stream
without doing heavy work. The actual data production happens when the stream
is polled. Do not block on async operations here -- build the stream and let
the runtime drive it.&lt;/p&gt;
&lt;h3 id="existing-implementations-to-learn-from_1"&gt;Existing Implementations to Learn From&lt;a class="headerlink" href="#existing-implementations-to-learn-from_1" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/stream/struct.StreamingTableExec.html"&gt;StreamingTableExec&lt;/a&gt;&lt;/strong&gt; -- Executes a streaming table scan. It takes a
  stream factory (a closure that produces streams) and handles partitioning.
  Good reference for wrapping external streams.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/datasource/struct.DataSourceExec.html"&gt;DataSourceExec&lt;/a&gt;&lt;/strong&gt; -- The execution plan behind DataFusion's built-in file
  scanning (Parquet, CSV, JSON). It demonstrates sophisticated partitioning,
  filter pushdown, and projection pushdown.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="layer-3-sendablerecordbatchstream"&gt;Layer 3: SendableRecordBatchStream&lt;a class="headerlink" href="#layer-3-sendablerecordbatchstream" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/execution/type.SendableRecordBatchStream.html"&gt;SendableRecordBatchStream&lt;/a&gt; is where the real work happens. It is defined as:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-rust"&gt;type SendableRecordBatchStream =
    Pin&amp;lt;Box&amp;lt;dyn RecordBatchStream&amp;lt;Item = Result&amp;lt;RecordBatch&amp;gt;&amp;gt; + Send&amp;gt;&amp;gt;;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This is an async stream of &lt;code&gt;RecordBatch&lt;/code&gt;es that can be sent across threads. When
the DataFusion runtime polls this stream, your code runs: reading files, calling
APIs, transforming data, etc.&lt;/p&gt;
&lt;h3 id="using-recordbatchstreamadapter"&gt;Using RecordBatchStreamAdapter&lt;a class="headerlink" href="#using-recordbatchstreamadapter" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The easiest way to create a &lt;code&gt;SendableRecordBatchStream&lt;/code&gt; is with
&lt;a href="https://docs.rs/datafusion/latest/datafusion/physical_plan/stream/struct.RecordBatchStreamAdapter.html"&gt;RecordBatchStreamAdapter&lt;/a&gt;. It bridges any &lt;code&gt;futures::Stream&amp;lt;Item =
Result&amp;lt;RecordBatch&amp;gt;&amp;gt;&lt;/code&gt; into the &lt;code&gt;SendableRecordBatchStream&lt;/code&gt; type:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-rust"&gt;use datafusion::physical_plan::stream::RecordBatchStreamAdapter;

fn execute(
    &amp;amp;self,
    partition: usize,
    context: Arc&amp;lt;TaskContext&amp;gt;,
) -&amp;gt; Result&amp;lt;SendableRecordBatchStream&amp;gt; {
    let schema = self.schema();
    let config = self.config.clone();

    let stream = futures::stream::once(async move {
        // ALL the heavy work happens here, inside the stream:
        // - Open connections
        // - Read data from external sources
        // - Transform and batch the results
        let batches = fetch_data_from_source(&amp;amp;config).await?;
        Ok(batches)
    })
    .flat_map(|result| match result {
        Ok(batch) =&amp;gt; futures::stream::iter(vec![Ok(batch)]),
        Err(e) =&amp;gt; futures::stream::iter(vec![Err(e)]),
    });

    Ok(Box::pin(RecordBatchStreamAdapter::new(schema, stream)))
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id="blocking-work-use-a-separate-thread-pool"&gt;Blocking Work: Use a Separate Thread Pool&lt;a class="headerlink" href="#blocking-work-use-a-separate-thread-pool" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;If your stream performs &lt;strong&gt;blocking&lt;/strong&gt; work -- such as blocking I/O, or CPU work
that runs for hundreds of milliseconds without yielding -- you must avoid
blocking the tokio async runtime. Short CPU work (e.g., parsing a batch in a
few milliseconds) is fine to do inline as long as your code yields back to the
runtime frequently. But for long-running synchronous work that cannot yield,
offload to a dedicated thread pool and send results back through a channel:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-rust"&gt;fn execute(
    &amp;amp;self,
    partition: usize,
    context: Arc&amp;lt;TaskContext&amp;gt;,
) -&amp;gt; Result&amp;lt;SendableRecordBatchStream&amp;gt; {
    let schema = self.schema();
    let config = self.config.clone();

    let (tx, rx) = tokio::sync::mpsc::channel(2);

    // Spawn blocking work on a dedicated thread pool
    tokio::task::spawn_blocking(move || {
        let batches = generate_data(&amp;amp;config);
        for batch in batches {
            if tx.blocking_send(Ok(batch)).is_err() {
                break; // Receiver dropped, query was cancelled
            }
        }
    });

    let stream = tokio_stream::wrappers::ReceiverStream::new(rx);
    Ok(Box::pin(RecordBatchStreamAdapter::new(schema, stream)))
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This pattern keeps the async runtime responsive while long-running synchronous
work runs on its own threads. For a working example that shows how to configure
separate thread pools for I/O and CPU work, see the
&lt;a href="https://github.com/apache/datafusion/blob/main/datafusion-examples/examples/query_planning/thread_pools.rs"&gt;thread_pools example&lt;/a&gt;
in the DataFusion repository.&lt;/p&gt;
&lt;h2 id="where-should-the-work-happen"&gt;Where Should the Work Happen?&lt;a class="headerlink" href="#where-should-the-work-happen" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;This table summarizes what belongs at each layer:&lt;/p&gt;
&lt;table class="table"&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Layer&lt;/th&gt;
&lt;th&gt;Runs During&lt;/th&gt;
&lt;th&gt;Should Do&lt;/th&gt;
&lt;th&gt;Should NOT Do&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;TableProvider::scan()&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Planning&lt;/td&gt;
&lt;td&gt;Build an &lt;code&gt;ExecutionPlan&lt;/code&gt; with metadata&lt;/td&gt;
&lt;td&gt;I/O, network calls, heavy computation&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;ExecutionPlan::execute()&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Execution (once per partition)&lt;/td&gt;
&lt;td&gt;Construct a stream, set up channels&lt;/td&gt;
&lt;td&gt;Block on async work, read data&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;RecordBatchStream&lt;/code&gt; (polling)&lt;/td&gt;
&lt;td&gt;Execution&lt;/td&gt;
&lt;td&gt;All I/O, computation, data production&lt;/td&gt;
&lt;td&gt;--&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The guiding principle: &lt;strong&gt;push work as late as possible.&lt;/strong&gt; Planning should be
fast so the optimizer can do its job. Execution setup should be fast so all
partitions can start promptly. The stream is where you spend time producing
data.&lt;/p&gt;
&lt;h3 id="why-this-matters"&gt;Why This Matters&lt;a class="headerlink" href="#why-this-matters" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;When &lt;code&gt;scan()&lt;/code&gt; does heavy work, several problems arise:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Planning becomes slow.&lt;/strong&gt; If a query touches 10 tables and each &lt;code&gt;scan()&lt;/code&gt;
   takes 500ms, planning alone takes 5 seconds before any data flows.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Execution is single-threaded.&lt;/strong&gt; &lt;code&gt;scan()&lt;/code&gt; runs on a single thread during
   planning, so any work done there cannot benefit from the parallel execution
   that DataFusion provides across partitions.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;The optimizer cannot help.&lt;/strong&gt; The optimizer runs between planning and
   execution. If you have already fetched data during planning, optimizations
   like predicate pushdown or partition pruning cannot reduce the work.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Resource management breaks down.&lt;/strong&gt; DataFusion manages concurrency and
   memory during execution. Work done during planning bypasses these controls.&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id="filter-pushdown-doing-less-work"&gt;Filter Pushdown: Doing Less Work&lt;a class="headerlink" href="#filter-pushdown-doing-less-work" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;One of the most impactful optimizations you can add to a custom table provider
is &lt;strong&gt;filter pushdown&lt;/strong&gt; -- letting the source skip data that the query does not
need, rather than reading everything and filtering it afterward.&lt;/p&gt;
&lt;h3 id="how-filter-pushdown-works"&gt;How Filter Pushdown Works&lt;a class="headerlink" href="#how-filter-pushdown-works" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;When DataFusion plans a query with a &lt;code&gt;WHERE&lt;/code&gt; clause, it passes the filter
predicates to your &lt;code&gt;scan()&lt;/code&gt; method as the &lt;code&gt;filters&lt;/code&gt; parameter. By default,
DataFusion assumes your provider cannot handle any filters and inserts a
&lt;code&gt;FilterExec&lt;/code&gt; node above your scan to apply them. But if your source &lt;em&gt;can&lt;/em&gt;
evaluate some predicates during scanning -- for example, by skipping files,
partitions, or row groups that cannot match -- you can eliminate a huge amount
of unnecessary I/O.&lt;/p&gt;
&lt;p&gt;To opt in, implement &lt;code&gt;supports_filters_pushdown&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-rust"&gt;fn supports_filters_pushdown(
    &amp;amp;self,
    filters: &amp;amp;[&amp;amp;Expr],
) -&amp;gt; Result&amp;lt;Vec&amp;lt;TableProviderFilterPushDown&amp;gt;&amp;gt; {
    Ok(filters.iter().map(|f| {
        match f {
            // We can fully evaluate equality filters on
            // the partition column at the source
            Expr::BinaryExpr(BinaryExpr {
                left, op: Operator::Eq, right
            }) if is_partition_column(left) || is_partition_column(right) =&amp;gt; {
                TableProviderFilterPushDown::Exact
            }
            // All other filters: let DataFusion handle them
            _ =&amp;gt; TableProviderFilterPushDown::Unsupported,
        }
    }).collect())
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The three possible responses for each filter are:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;Exact&lt;/code&gt;&lt;/strong&gt; -- Your source guarantees that no output rows will have a false
  value for this predicate. Because the filter is fully evaluated at the source,
  DataFusion will &lt;strong&gt;not&lt;/strong&gt; add a &lt;code&gt;FilterExec&lt;/code&gt; for it.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;Inexact&lt;/code&gt;&lt;/strong&gt; -- Your source has the ability to reduce the data produced, but
  the output may still include rows that do not satisfy the predicate. For
  example, you might skip entire files based on metadata statistics but not
  filter individual rows within a file. DataFusion will still add a &lt;code&gt;FilterExec&lt;/code&gt;
  above your scan to remove any remaining rows that slipped through.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;Unsupported&lt;/code&gt;&lt;/strong&gt; -- Your source ignores this filter entirely. DataFusion
  handles it.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="why-filter-pushdown-matters"&gt;Why Filter Pushdown Matters&lt;a class="headerlink" href="#why-filter-pushdown-matters" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Consider a table with 1 billion rows partitioned by &lt;code&gt;region&lt;/code&gt;, and a query:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-sql"&gt;SELECT * FROM events WHERE region = 'us-east-1' AND event_type = 'click';
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Without filter pushdown:&lt;/strong&gt; Your table provider reads all 1 billion rows
across all regions. DataFusion then applies both filters, discarding the vast
majority of the data.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;With filter pushdown on &lt;code&gt;region&lt;/code&gt;:&lt;/strong&gt; Your &lt;code&gt;scan()&lt;/code&gt; method sees the
&lt;code&gt;region = 'us-east-1'&lt;/code&gt; filter and constructs an execution plan that only reads
the &lt;code&gt;us-east-1&lt;/code&gt; partition. If that partition holds 100 million rows, you have
just eliminated 90% of the I/O. DataFusion still applies the &lt;code&gt;event_type&lt;/code&gt;
filter via &lt;code&gt;FilterExec&lt;/code&gt; if you reported it as &lt;code&gt;Unsupported&lt;/code&gt;.&lt;/p&gt;
&lt;h3 id="only-push-down-filters-when-the-data-source-can-do-better"&gt;Only Push Down Filters When the Data Source Can Do Better&lt;a class="headerlink" href="#only-push-down-filters-when-the-data-source-can-do-better" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;DataFusion already pushes filters as close to the data source as possible, typically placing them directly above the scan. &lt;code&gt;FilterExec&lt;/code&gt; is also highly optimized, with vectorized evaluation and type-specialized kernels for fast predicate evaluation.&lt;/p&gt;
&lt;p&gt;Because of this, you should only implement filter pushdown when your data source
can do strictly better -- for example, by avoiding I/O entirely through
skipping files or partitions based on metadata. If your data source cannot
eliminate I/O in this way, it is usually better to let DataFusion handle the
filter, as its in-memory execution is already highly efficient.&lt;/p&gt;
&lt;h3 id="using-explain-to-debug-your-table-provider"&gt;Using EXPLAIN to Debug Your Table Provider&lt;a class="headerlink" href="#using-explain-to-debug-your-table-provider" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The &lt;code&gt;EXPLAIN&lt;/code&gt; statement is your best tool for understanding what DataFusion is
actually doing with your table provider. It shows the physical plan that
DataFusion will execute, including any operators it inserted:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-sql"&gt;EXPLAIN SELECT * FROM events WHERE region = 'us-east-1' AND event_type = 'click';
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If you are using DataFrames, call &lt;code&gt;.explain(false, false)&lt;/code&gt; for the logical plan
or &lt;code&gt;.explain(false, true)&lt;/code&gt; for the physical plan. You can also print the plans
in verbose mode with &lt;code&gt;.explain(true, true)&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Before filter pushdown&lt;/strong&gt;, the plan might look like:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-text"&gt;FilterExec: region@0 = us-east-1 AND event_type@1 = click
  MyExecPlan: partitions=50
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Here DataFusion is reading all 50 partitions and filtering everything
afterward. The &lt;code&gt;FilterExec&lt;/code&gt; above your scan is doing all the predicate work.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;After implementing pushdown for &lt;code&gt;region&lt;/code&gt;&lt;/strong&gt; (reported as &lt;code&gt;Exact&lt;/code&gt;):&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-text"&gt;FilterExec: event_type@1 = click
  MyExecPlan: partitions=5, filter=[region = us-east-1]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Now your exec reads only the 5 partitions for &lt;code&gt;us-east-1&lt;/code&gt;, and the remaining
&lt;code&gt;FilterExec&lt;/code&gt; only handles the &lt;code&gt;event_type&lt;/code&gt; predicate. The &lt;code&gt;region&lt;/code&gt; filter has
been fully absorbed by your scan.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;After implementing pushdown for both filters&lt;/strong&gt; (both &lt;code&gt;Exact&lt;/code&gt;):&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-text"&gt;MyExecPlan: partitions=5, filter=[region = us-east-1 AND event_type = click]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;No &lt;code&gt;FilterExec&lt;/code&gt; at all -- your source handles everything.&lt;/p&gt;
&lt;p&gt;Similarly, &lt;code&gt;EXPLAIN&lt;/code&gt; will reveal whether DataFusion is inserting unnecessary
&lt;code&gt;SortExec&lt;/code&gt; or &lt;code&gt;RepartitionExec&lt;/code&gt; nodes that you could eliminate by declaring
better output properties. Whenever your queries seem slower than expected,
&lt;code&gt;EXPLAIN&lt;/code&gt; is the first place to look.&lt;/p&gt;
&lt;h3 id="a-complete-filter-pushdown-example"&gt;A Complete Filter Pushdown Example&lt;a class="headerlink" href="#a-complete-filter-pushdown-example" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;To make filter pushdown concrete, here is an illustrative example. Imagine a
table provider that reads from a set of date-partitioned directories on disk
(e.g., &lt;code&gt;data/2026-03-01/&lt;/code&gt;, &lt;code&gt;data/2026-03-02/&lt;/code&gt;, ...). Each directory contains
one or more Parquet files for that date. By pushing down a filter on the &lt;code&gt;date&lt;/code&gt;
column, the provider can skip entire directories -- avoiding the I/O of listing
and reading files that cannot possibly match the query.&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-rust"&gt;/// A table provider backed by date-partitioned directories.
/// Each date directory contains data files; by filtering on the
/// `date` column we can skip entire directories of I/O.
struct DatePartitionedTable {
    schema: SchemaRef,
    /// Maps date strings ("2026-03-01") to directory paths
    partitions: HashMap&amp;lt;String, String&amp;gt;,
}

#[async_trait::async_trait]
impl TableProvider for DatePartitionedTable {
    fn as_any(&amp;amp;self) -&amp;gt; &amp;amp;dyn Any { self }
    fn schema(&amp;amp;self) -&amp;gt; SchemaRef { Arc::clone(&amp;amp;self.schema) }
    fn table_type(&amp;amp;self) -&amp;gt; TableType { TableType::Base }

    fn supports_filters_pushdown(
        &amp;amp;self,
        filters: &amp;amp;[&amp;amp;Expr],
    ) -&amp;gt; Result&amp;lt;Vec&amp;lt;TableProviderFilterPushDown&amp;gt;&amp;gt; {
        Ok(filters.iter().map(|f| {
            if Self::is_date_equality_filter(f) {
                // We can fully evaluate this: we will only read
                // directories matching the date, so no rows with
                // a different date will appear in the output.
                TableProviderFilterPushDown::Exact
            } else {
                TableProviderFilterPushDown::Unsupported
            }
        }).collect())
    }

    async fn scan(
        &amp;amp;self,
        _state: &amp;amp;dyn Session,
        projection: Option&amp;lt;&amp;amp;Vec&amp;lt;usize&amp;gt;&amp;gt;,
        filters: &amp;amp;[Expr],
        limit: Option&amp;lt;usize&amp;gt;,
    ) -&amp;gt; Result&amp;lt;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt; {
        // Determine which date partitions to read by inspecting
        // the pushed-down filters. This is the key optimization:
        // we decide *during planning* which directories to scan,
        // so that execution never touches irrelevant data.
        let dates_to_read: Vec&amp;lt;String&amp;gt; = self
            .extract_date_values(filters)
            .unwrap_or_else(||
                self.partitions.keys().cloned().collect()
            );

        let dirs: Vec&amp;lt;String&amp;gt; = dates_to_read
            .iter()
            .filter_map(|d| self.partitions.get(d).cloned())
            .collect();
        let num_dirs = dirs.len();

        Ok(Arc::new(DatePartitionedExec {
            schema: Arc::clone(&amp;amp;self.schema),
            directories: dirs,
            properties: PlanProperties::new(
                EquivalenceProperties::new(
                    Arc::clone(&amp;amp;self.schema),
                ),
                // One partition per date directory -- these
                // will be read in parallel.
                Partitioning::UnknownPartitioning(num_dirs),
                EmissionType::Incremental,
                Boundedness::Bounded,
            ),
        }))
    }
}

impl DatePartitionedTable {
    /// Check if a filter is an equality comparison on the `date` column.
    fn is_date_equality_filter(expr: &amp;amp;Expr) -&amp;gt; bool {
        // In practice, match on BinaryExpr { left, op: Eq, right }
        // and check if either side references the "date" column.
        // Simplified here for clarity.
        todo!("match on date equality expressions")
    }

    /// Extract date literal values from pushed-down equality filters.
    fn extract_date_values(&amp;amp;self, filters: &amp;amp;[Expr]) -&amp;gt; Option&amp;lt;Vec&amp;lt;String&amp;gt;&amp;gt; {
        // Parse filters like `date = '2026-03-01'` and return
        // the literal date strings. Returns None if no date
        // filters are present (meaning: read all partitions).
        todo!("extract date literals from filter expressions")
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The key insight is that the filter pushdown decision (&lt;code&gt;supports_filters_pushdown&lt;/code&gt;)
and the partition pruning (&lt;code&gt;scan()&lt;/code&gt;) work together: the first tells DataFusion
that a &lt;code&gt;FilterExec&lt;/code&gt; is unnecessary for the &lt;code&gt;date&lt;/code&gt; predicate, and the second
ensures that only the relevant directories are scanned. The actual file reading
happens later, in the stream produced by &lt;code&gt;execute()&lt;/code&gt;.&lt;/p&gt;
&lt;h2 id="putting-it-all-together"&gt;Putting It All Together&lt;a class="headerlink" href="#putting-it-all-together" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;p&gt;Here is a minimal but complete example of a custom table provider that generates
data lazily during streaming:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-rust"&gt;use std::any::Any;
use std::sync::Arc;

use arrow::array::Int64Array;
use arrow::datatypes::{DataType, Field, Schema, SchemaRef};
use arrow::record_batch::RecordBatch;
use datafusion::catalog::TableProvider;
use datafusion::common::Result;
use datafusion::datasource::TableType;
use datafusion::catalog::Session;
use datafusion::execution::SendableRecordBatchStream;
use datafusion::logical_expr::Expr;
use datafusion::physical_expr::EquivalenceProperties;
use datafusion::physical_plan::execution_plan::{Boundedness, EmissionType};
use datafusion::physical_plan::stream::RecordBatchStreamAdapter;
use datafusion::physical_plan::{
    ExecutionPlan, Partitioning, PlanProperties,
};
use futures::stream;

/// A table provider that generates sequential numbers on demand.
struct CountingTable {
    schema: SchemaRef,
    num_partitions: usize,
    rows_per_partition: usize,
}

impl CountingTable {
    fn new(num_partitions: usize, rows_per_partition: usize) -&amp;gt; Self {
        let schema = Arc::new(Schema::new(vec![
            Field::new("partition", DataType::Int64, false),
            Field::new("value", DataType::Int64, false),
        ]));
        Self { schema, num_partitions, rows_per_partition }
    }
}

#[async_trait::async_trait]
impl TableProvider for CountingTable {
    fn as_any(&amp;amp;self) -&amp;gt; &amp;amp;dyn Any { self }
    fn schema(&amp;amp;self) -&amp;gt; SchemaRef { Arc::clone(&amp;amp;self.schema) }
    fn table_type(&amp;amp;self) -&amp;gt; TableType { TableType::Base }

    async fn scan(
        &amp;amp;self,
        _state: &amp;amp;dyn Session,
        projection: Option&amp;lt;&amp;amp;Vec&amp;lt;usize&amp;gt;&amp;gt;,
        _filters: &amp;amp;[Expr],
        limit: Option&amp;lt;usize&amp;gt;,
    ) -&amp;gt; Result&amp;lt;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt; {
        // Light work only: build the plan with metadata
        Ok(Arc::new(CountingExec {
            schema: Arc::clone(&amp;amp;self.schema),
            num_partitions: self.num_partitions,
            rows_per_partition: limit
                .unwrap_or(self.rows_per_partition)
                .min(self.rows_per_partition),
            properties: PlanProperties::new(
                EquivalenceProperties::new(Arc::clone(&amp;amp;self.schema)),
                Partitioning::UnknownPartitioning(self.num_partitions),
                EmissionType::Incremental,
                Boundedness::Bounded,
            ),
        }))
    }
}

struct CountingExec {
    schema: SchemaRef,
    num_partitions: usize,
    rows_per_partition: usize,
    properties: PlanProperties,
}

impl ExecutionPlan for CountingExec {
    fn name(&amp;amp;self) -&amp;gt; &amp;amp;str { "CountingExec" }
    fn as_any(&amp;amp;self) -&amp;gt; &amp;amp;dyn Any { self }
    fn properties(&amp;amp;self) -&amp;gt; &amp;amp;PlanProperties { &amp;amp;self.properties }
    fn children(&amp;amp;self) -&amp;gt; Vec&amp;lt;&amp;amp;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt; { vec![] }

    fn with_new_children(
        self: Arc&amp;lt;Self&amp;gt;,
        _children: Vec&amp;lt;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt;,
    ) -&amp;gt; Result&amp;lt;Arc&amp;lt;dyn ExecutionPlan&amp;gt;&amp;gt; {
        Ok(self)
    }

    fn execute(
        &amp;amp;self,
        partition: usize,
        _context: Arc&amp;lt;TaskContext&amp;gt;,
    ) -&amp;gt; Result&amp;lt;SendableRecordBatchStream&amp;gt; {
        let schema = Arc::clone(&amp;amp;self.schema);
        let rows = self.rows_per_partition;

        // The heavy work (data generation) happens inside the stream,
        // not here in execute().
        let batch_stream = stream::once(async move {
            let partitions = Int64Array::from(
                vec![partition as i64; rows],
            );
            let values = Int64Array::from(
                (0..rows as i64).collect::&amp;lt;Vec&amp;lt;_&amp;gt;&amp;gt;(),
            );
            let batch = RecordBatch::try_new(
                Arc::clone(&amp;amp;schema),
                vec![Arc::new(partitions), Arc::new(values)],
            )?;
            Ok(batch)
        });

        Ok(Box::pin(RecordBatchStreamAdapter::new(
            Arc::clone(&amp;amp;self.schema),
            batch_stream,
        )))
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id="acknowledgements"&gt;Acknowledgements&lt;a class="headerlink" href="#acknowledgements" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;I would like to thank &lt;a href="https://rerun.io"&gt;Rerun.io&lt;/a&gt; for sponsoring the development of this work. Rerun is
building a data visualization system for Physical AI and makes heavy use of DataFusion
table providers for working with data analytics.&lt;/p&gt;
&lt;p&gt;I would also like to thank the reviewers of this post for their helpful feedback and
suggestions: &lt;a href="https://github.com/2010YOUY01"&gt;@2010YOUY01&lt;/a&gt;, &lt;a href="https://github.com/adriangb"&gt;@adriangb&lt;/a&gt;, &lt;a href="https://github.com/alamb"&gt;@alamb&lt;/a&gt;, &lt;a href="https://github.com/kevinjqliu"&gt;@kevinjqliu&lt;/a&gt;, &lt;a href="https://github.com/Omega359"&gt;@Omega359&lt;/a&gt;,
&lt;a href="https://github.com/pgwhalen"&gt;@pgwhalen&lt;/a&gt;, and &lt;a href="https://github.com/stuhood"&gt;@stuhood&lt;/a&gt;.&lt;/p&gt;
&lt;h2 id="get-involved"&gt;Get Involved&lt;a class="headerlink" href="#get-involved" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;DataFusion is not a project built or driven by a single person, company, or
foundation. Our community of users and contributors works together to build a
shared technology that none of us could have built alone.&lt;/p&gt;
&lt;p&gt;If you are interested in joining us, we would love to have you. You can try out
DataFusion on some of your own data and projects and let us know how it goes,
contribute suggestions, documentation, bug reports, or a PR with documentation,
tests, or code. A list of open issues suitable for beginners is &lt;a href="https://github.com/apache/datafusion/issues?q=is%3Aissue+is%3Aopen+label%3A%22good+first+issue%22"&gt;here&lt;/a&gt;, and you
can find out how to reach us on the &lt;a href="https://datafusion.apache.org/contributor-guide/communication.html"&gt;communication doc&lt;/a&gt;.&lt;/p&gt;
&lt;h2 id="further-reading"&gt;Further Reading&lt;a class="headerlink" href="#further-reading" title="Permanent link"&gt;¶&lt;/a&gt;&lt;/h2&gt;
&lt;hr/&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/catalog/trait.TableProvider.html"&gt;&lt;code&gt;TableProvider&lt;/code&gt; API docs&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/physical_plan/trait.ExecutionPlan.html"&gt;&lt;code&gt;ExecutionPlan&lt;/code&gt; API docs&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://docs.rs/datafusion/latest/datafusion/execution/type.SendableRecordBatchStream.html"&gt;&lt;code&gt;SendableRecordBatchStream&lt;/code&gt; API docs&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://github.com/apache/datafusion/tree/main/datafusion-examples/examples"&gt;DataFusion examples directory&lt;/a&gt; --
  contains working examples including custom table providers&lt;/li&gt;
&lt;/ul&gt;
&lt;hr/&gt;
&lt;p&gt;&lt;em&gt;Note: Portions of this blog post were written with the assistance of an AI agent.&lt;/em&gt;&lt;/p&gt;</content><category term="blog"/></entry></feed>